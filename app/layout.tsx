import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Free Influencer Campaign Report Generator | Winfluencer',
  description: 'Generate professional influencer marketing campaign reports in seconds. Track engagement, CPM, and ROI across multiple posts and influencers.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
